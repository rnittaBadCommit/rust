# 2026-04-05

## Today's Focus

- `rust_from_c_guide.md` を `knowledge/` 配下へ移し、知識ベースの入口と運用ルールを整えた
- `AGENTS.md` と `.codex/` の起動時読込ルール、知識管理ルールを明文化した
- `ex03_review` を中心に、`Option`, `Vec`, `match`, `&mut Vec<T>`, `Result` を使う課題を進めた

## Learned

- `knowledge/README.md` と `knowledge/not_yet_internalized/README.md` を用意すると、主教材と「まだ曖昧な知識」を分けて管理しやすい
- `&mut` 参照を別変数に束縛し直す試行で、参照型そのものと中身の値は別物なので、そのまま代入はできない
- `split_key_value_2` は値だけでなくキー側の空文字も弾く必要がある
- `clamp_scores` は `for x in xs` でも `iter_mut().for_each(...)` でも、その場更新で書ける
- `Stack` は `Vec` の `push`, `pop`, `last`, `len` をそのまま使うと素直に実装できる
- `Packet` の `match` で enum を借用のまま読み分けながら文字列を組み立てられる
- `read_to_string(path)?` を使う入り口までは書けたが、空白行を除いた行数カウントはまだ途中

## Questions

- `let mut v2 = &mut v; let mut v3 = v2;` のあとで、どの束縛が実際に借用を持っているかをもっと正確に説明できるようにしたい
- `&mut Vec<T>` を受けたとき、`for x in xs` と `xs.iter_mut()` をどう使い分けると読みやすいか
- `Packet` の文字列組み立ては `String` の `+` 連結より `format!` の方が自然か
- `todo!()` を残したまま途中の試行コードを置くとき、どの段階で消すのがよいか

## Next

- `ex03_review/04_nonempty_line_count` を最後まで実装してテストを通す
- `Packet` の文字列組み立てを、より Rust らしい書き方でも試す
- `knowledge/` 配下の索引とノートを増やし、trait 周りの理解を辿りやすくする

## Links

- [[Home]]
- [[knowledge/rust_from_c_guide]]
- [[knowledge/not_yet_internalized/trait_basics]]
- [[ex03_review/README]]
